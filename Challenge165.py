# Tui Popenoe
# Challenge 165 Game of Life

import sys

def createGrid(x, y):
    grid = [[0 for i in xrange(x)] for j in xrange(y)]

def iterate(n, grid):
    for i in len(grid):
        for j in len(grid[0]):

    def getNeighbors():
        


def main():
    grid = createGrid(sys,argv[2], argv[3])